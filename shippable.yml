language: php

php:
  - 5.4

before_script:
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage
  - composer self-update # Bring composer up to latest version.
  - composer install # Since we are deploying in production
  - mysql -e 'create database if not exists test;'
  
script:
  - phpunit --log-junit shippable/testresults/junit.xml --coverage-xml shippable/codecoverage test.php
